name: ci

on:
  push:

jobs:
  phpunit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup
        run: make install
      - name: Run phpunit
        run: make test
